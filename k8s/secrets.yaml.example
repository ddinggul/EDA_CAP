# Kubernetes Secret 예제 파일
# 실제 사용 시 secrets.yaml로 복사하고 base64로 인코딩된 값을 입력하세요
#
# Base64 인코딩 방법:
#   echo -n "your-secret-key" | base64
#
# 주의: 이 파일을 Git에 커밋하지 마세요!

apiVersion: v1
kind: Secret
metadata:
  name: toefl-secrets
  labels:
    app: toefl-evaluator
type: Opaque
data:
  # Base64로 인코딩된 Naver Clova Secret Key
  # 예: echo -n "your_clova_key" | base64
  clova-secret-key: "YOUR_BASE64_ENCODED_CLOVA_KEY"

  # Base64로 인코딩된 OpenAI API Key
  # 예: echo -n "sk-your_openai_key" | base64
  openai-api-key: "YOUR_BASE64_ENCODED_OPENAI_KEY"

---
# 또는 stringData를 사용하면 자동으로 base64 인코딩됩니다 (권장)
apiVersion: v1
kind: Secret
metadata:
  name: toefl-secrets
  labels:
    app: toefl-evaluator
type: Opaque
stringData:
  clova-secret-key: "your_clova_secret_key_here"
  openai-api-key: "sk-your_openai_api_key_here"
