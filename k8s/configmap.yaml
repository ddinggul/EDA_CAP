apiVersion: v1
kind: ConfigMap
metadata:
  name: toefl-config
  labels:
    app: toefl-evaluator
data:
  # OpenAI 모델 설정 (선택사항)
  openai-model: "gpt-3.5-turbo"

  # Flask API 설정
  flask-api-host: "0.0.0.0"
  flask-api-port: "5000"

  # FastAPI 설정
  backend-host: "0.0.0.0"
  backend-port: "8000"
